{
  "name": "readmark",
  "version": "1.0.0",
  "private": true,
  "description": "An elegant, iOS-Books-style Markdown reader — paste Markdown or a GitHub URL and read it beautifully.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "bun test src/core",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.4",
    "@types/bun": "latest",
    "oxfmt": "^0.58.0",
    "oxlint": "^1.73.0",
    "svelte": "^5.46.4",
    "svelte-check": "^4.1.4",
    "typescript": "^5.7.3",
    "vite": "^8.1.3",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "engines": {
    "node": ">=24"
  }
}
